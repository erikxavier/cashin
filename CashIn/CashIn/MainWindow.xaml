<Window x:Class="CashIn.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CashIn" Height="350" Width="525" Loaded="Window_Loaded">
    <Grid>
        <DataGrid Height="200" HorizontalAlignment="Left" Margin="12,12,0,0" Name="dataGrid1" VerticalAlignment="Top" Width="200" ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Codigo" Binding="{Binding IDOrcamento}" />
                <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente.Pessoa.Nome}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
